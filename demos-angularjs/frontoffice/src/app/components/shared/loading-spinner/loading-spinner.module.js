import loadingSpinner from './loading-spinner.component';

export default angular.module('LeadEssentialsFO.components.loadingSpinner', [])
    .component('loadingSpinnerComponent', loadingSpinner)
    .name;
