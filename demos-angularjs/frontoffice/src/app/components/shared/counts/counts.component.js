import template from './counts.template.html';
import controller from './counts.controller';

export default {
    template,
    controller,
    bindings: {
        additionalClass: '@'
    }
};
