import template from './content-bundle.template.html';
import controller from './content-bundle.controller';

export default {
    template,
    controller,
    bindings: {
        data: '<'
    }};
