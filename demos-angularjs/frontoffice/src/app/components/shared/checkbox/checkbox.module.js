import checkboxComponent from './checkbox.component';

export default angular.module('LeadEssentialsFO.components.checkboxComponent', [])
    .component('checkboxComponent', checkboxComponent)
    .name;
