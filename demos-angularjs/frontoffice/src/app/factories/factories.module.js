import angular from 'angular';

// import httpInterceptor from './http-interceptor.factory';

export default angular
    .module('LeadEssentialsFO.factories', [])
    // .factory('httpInterceptor', httpInterceptor)
    .name;
